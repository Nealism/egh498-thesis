[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "egh498-thesis"
version = "0.1.0"

[tool.setuptools]
py-modules = ["default_arguments"]

[tool.setuptools.packages.find]
where = ["."]
include = ["maviper*", "assets*"]