TODO:
    -read and understand how trees are loaded/saves
        -how and why he says them to the scratch1 directory
        -how then loads entire scene_0.xml into directory
            -THEN, we can go about loading terrains.xml and its assets
    -ask brendan about grass stuff (how is it incorporated into the image)
    -figure out how we incorporate the idea of a goal
        -is the goal position gonna be an input to network?

    -try and load/train the bot on the perception net
        -ie: incorporate the ground truth image
    -ascertain mapping between PNG coords and mujoco coords
        -every step (or every 10 steps, or whatever honestly):
            -get robot pos
            -convert that to an index in the image
            -compute the top-left/bot-right of sub-section
                -also get the sub-section and make it its own PNG (obviously)
            -display base image with bounding box around sub-section
            -monitor to see its correct
                -can just start the camera birds eye

Ideas about terrain generation:
    -make one file (terrain.py) hold two classes:
        Terrain: (takes a numpy array as an argument)
            self.arr (image in form of numpy array)
            self.image (PNG form of image)

            load_im_from_arr() - called in init, just generates self.image
            compute_sub_section() - computes it and returns the image
            display_map() - displays image with bounding box
            top_left_bot_right()

        TerrainGen:
            just a class to generate numpy arrays that give certain terrains
            
